{
  "id": "cliq",
  "kind": "channel",
  "channels": ["cliq"],
  "name": "Zoho Cliq",
  "description": "Native Zoho Cliq channel for OpenClaw — reply to DMs and channel @mentions as a bot, with streaming previews, cards, and message actions.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "clientId": { "type": "string" },
      "clientSecret": { "type": "string" },
      "botId": { "type": "string" },
      "botName": { "type": "string" },
      "webhookSecret": { "type": "string" },
      "refreshToken": { "type": "string" },
      "allowFrom": {
        "type": "array",
        "items": { "type": "string" }
      },
      "selfSenderIds": {
        "type": "array",
        "items": { "type": "string" }
      },
      "dmPolicy": {
        "type": "string",
        "enum": ["open", "allowlist", "pairing", "disabled"]
      },
      "ackPolicy": {
        "type": "string",
        "enum": ["after_dispatch", "immediate"],
        "default": "after_dispatch"
      },
      "replyToMode": {
        "type": "string",
        "enum": ["off", "first", "all", "batched"],
        "default": "off"
      },
      "replyToModeByChatType": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "direct": { "type": "string", "enum": ["off", "first", "all", "batched"] },
          "group": { "type": "string", "enum": ["off", "first", "all", "batched"] },
          "channel": { "type": "string", "enum": ["off", "first", "all", "batched"] }
        }
      },
      "groupPolicy": {
        "type": "string",
        "enum": ["open", "disabled", "allowlist"]
      },
      "groups": {
        "type": "object",
        "propertyNames": { "type": "string" },
        "additionalProperties": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "requireMention": { "type": "boolean" },
            "ingest": { "type": "boolean" },
            "tools": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "allow": { "type": "array", "items": { "type": "string" } },
                "alsoAllow": { "type": "array", "items": { "type": "string" } },
                "deny": { "type": "array", "items": { "type": "string" } }
              }
            },
            "toolsBySender": {
              "type": "object",
              "propertyNames": { "type": "string" },
              "additionalProperties": {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "allow": { "type": "array", "items": { "type": "string" } },
                  "alsoAllow": { "type": "array", "items": { "type": "string" } },
                  "deny": { "type": "array", "items": { "type": "string" } }
                }
              }
            }
          }
        }
      },
      "streaming": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "preview": { "type": "string", "enum": ["on", "off"], "default": "off" }
        }
      },
      "reactions": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "agentGuidance": {
            "type": "string",
            "enum": ["minimal", "extensive", "off"],
            "default": "minimal"
          }
        }
      },
      "thinking": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "mode": { "type": "string", "enum": ["off", "placeholder"], "default": "off" },
          "text": { "type": "string", "default": "💭 …" }
        }
      },
      "welcome": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "enabled": { "type": "boolean", "default": false },
          "text": { "type": "string" },
          "textRejoin": { "type": "string" }
        }
      },
      "apiBase": { "type": "string" },
      "oauthBase": { "type": "string" },
      "apiVersion": {
        "type": "string",
        "enum": ["v2", "v3"],
        "default": "v2"
      }
    }
  },
  "channelConfigs": {
    "cliq": {
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "clientId": { "type": "string" },
          "clientSecret": { "type": "string" },
          "botId": { "type": "string" },
          "botName": { "type": "string" },
          "webhookSecret": { "type": "string" },
          "refreshToken": { "type": "string" },
          "allowFrom": {
            "type": "array",
            "items": { "type": "string" }
          },
          "selfSenderIds": {
            "type": "array",
            "items": { "type": "string" }
          },
          "dmPolicy": {
            "type": "string",
            "enum": ["open", "allowlist", "pairing", "disabled"]
          },
          "ackPolicy": {
            "type": "string",
            "enum": ["after_dispatch", "immediate"],
            "default": "after_dispatch"
          },
          "replyToMode": {
            "type": "string",
            "enum": ["off", "first", "all", "batched"],
            "default": "off"
          },
          "replyToModeByChatType": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "direct": { "type": "string", "enum": ["off", "first", "all", "batched"] },
              "group": { "type": "string", "enum": ["off", "first", "all", "batched"] },
              "channel": { "type": "string", "enum": ["off", "first", "all", "batched"] }
            }
          },
          "groupPolicy": {
            "type": "string",
            "enum": ["open", "disabled", "allowlist"]
          },
          "groups": {
            "type": "object",
            "propertyNames": { "type": "string" },
            "additionalProperties": {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "requireMention": { "type": "boolean" },
                "ingest": { "type": "boolean" },
                "tools": {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "allow": { "type": "array", "items": { "type": "string" } },
                    "alsoAllow": { "type": "array", "items": { "type": "string" } },
                    "deny": { "type": "array", "items": { "type": "string" } }
                  }
                },
                "toolsBySender": {
                  "type": "object",
                  "propertyNames": { "type": "string" },
                  "additionalProperties": {
                    "type": "object",
                    "additionalProperties": false,
                    "properties": {
                      "allow": { "type": "array", "items": { "type": "string" } },
                      "alsoAllow": { "type": "array", "items": { "type": "string" } },
                      "deny": { "type": "array", "items": { "type": "string" } }
                    }
                  }
                }
              }
            }
          },
          "streaming": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "preview": { "type": "string", "enum": ["on", "off"], "default": "off" }
            }
          },
          "reactions": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "agentGuidance": {
                "type": "string",
                "enum": ["minimal", "extensive", "off"],
                "default": "minimal"
              }
            }
          },
          "thinking": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "mode": { "type": "string", "enum": ["off", "placeholder"], "default": "off" },
              "text": { "type": "string", "default": "💭 …" }
            }
          },
          "welcome": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "enabled": { "type": "boolean", "default": false },
              "text": { "type": "string" },
              "textRejoin": { "type": "string" }
            }
          },
          "apiBase": { "type": "string" },
          "oauthBase": { "type": "string" },
          "apiVersion": {
            "type": "string",
            "enum": ["v2", "v3"],
            "default": "v2"
          }
        },
        "required": ["clientId", "clientSecret", "botId"]
      },
      "uiHints": {
        "clientId": { "label": "OAuth client id" },
        "clientSecret": { "label": "OAuth client secret", "sensitive": true },
        "botId": { "label": "Bot unique name" },
        "botName": { "label": "Bot display name" },
        "webhookSecret": { "label": "Webhook shared secret", "sensitive": true },
        "refreshToken": { "label": "User-context OAuth refresh token (required for channel posts / edits)", "sensitive": true },
        "allowFrom": { "label": "Allowed senders (Zoho Cliq user ids)" },
        "selfSenderIds": { "label": "Bot sender ids to ignore (self / other bots)" },
        "dmPolicy": { "label": "DM admission policy", "options": ["open", "allowlist", "pairing", "disabled"] },
        "ackPolicy": { "label": "Webhook ack timing", "options": ["after_dispatch", "immediate"] },
        "replyToMode": { "label": "Reply-to metadata mode (Cliq cannot render reply quotes; off = none, all = carry inbound id for correlation)", "options": ["off", "first", "all", "batched"] },
        "replyToModeByChatType": { "label": "Per-chat-type reply-to mode override (direct / group / channel)" },
        "groupPolicy": { "label": "Group admission policy", "options": ["open", "allowlist", "disabled"] },
        "groups": { "label": "Per-group config (keyed by channel unique name)" },
        "streaming": { "label": "Streaming preview", "options": ["off", "on"] },
        "reactions": { "label": "Agent reaction guidance", "options": ["minimal", "extensive", "off"] },
        "thinking": { "label": "Instant acknowledgement (thinking placeholder)", "options": ["off", "placeholder"] },
        "welcome": { "label": "Welcome message on subscribe (greeting DM on first contact)" },
        "oauthBase": { "label": "OAuth base URL (data center — defaults to EU https://accounts.zoho.eu)" },
        "apiBase": { "label": "Cliq API base URL (data center — defaults to EU https://cliq.zoho.eu)" },
        "apiVersion": { "label": "REST API generation for channel text posts + bot DMs (v2 default; v3 opt-in uses Webhooks.CREATE scope, no refresh token needed)", "options": ["v2", "v3"] }
      }
    }
  }
}
